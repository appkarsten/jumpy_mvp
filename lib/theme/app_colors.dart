import 'package:flutter/material.dart';

abstract class AppColors {
  static const primary = Colors.black45;
  static const accentColor = Color(0xFF9E5499);
  static const headlineColor = accentColor;
  static const animalColor = Color.fromARGB(255, 203, 163, 200);

  static const mainBackground = Colors.white;
  static const textColor = Color(0xFFA6A6A6);

  static const cardTitleLarge = Color(0xFFFAA023);
  static const cardTitleMedium = Color(0xFFA6A6A6);

  static const boxShadow = Color(0xFFF0F0F0);
  static const boxBorder = Color(0xFFF0F0F0);
}
